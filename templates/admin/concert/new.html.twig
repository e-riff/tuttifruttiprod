{% extends 'base.html.twig' %}

{% block title %}{{ 'admin.concert.create'|trans }}{% endblock %}

{% block body %}
<main class="adminForms container-md">
    <h1 class="text-center"><span class="text-primary">{{ 'admin.concert.create'|trans }}</span></h1>

    {{ include('admin/concert/_form.html.twig') }}

    <div class="mt-2">
    <a class="btn btn-primary me-1" href="{{ path('admin_concert_index') }}">{{ 'action.back_to_concerts'|trans }}</a>

    </div>
</main>
{% endblock %}
