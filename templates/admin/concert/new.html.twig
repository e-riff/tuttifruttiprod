{% extends 'base.html.twig' %}

{% block title %}New Concert{% endblock %}

{% block body %}
<main class="adminForms container-md">
    <h1 class="text-center"><span class="text-primary">Ajout d'un concert</span></h1>

    {{ include('admin/concert/_form.html.twig') }}

    <div class="mt-2">
    <a class="btn btn-primary me-1" href="{{ path('admin_concert_index') }}">retour aux concerts </a>

    </div>

{% endblock %}