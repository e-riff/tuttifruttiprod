{% extends 'base.html.twig' %}

{% block title %}Edit Concert{% endblock %}

{% block body %}
    <main class="adminForms container-md">

        <h1 class="text-center">Modification d'un concert</h1>

        {{ include('admin/concert/_form.html.twig', {'button_label': 'Valider'}) }}


        <div class="d-flex flex-row justify-content-start">
        {{ include('admin/concert/_delete_form.html.twig') }}
        </div>
            <a class="btn btn-primary me-1 mt-2" href="{{ path('admin_concert_index') }}">retour aux concerts </a>

    </main>
{% endblock %}
