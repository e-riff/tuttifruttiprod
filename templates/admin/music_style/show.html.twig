{% extends 'base.html.twig' %}

{% block title %}MusicStyle{% endblock %}

{% block body %}
    <h1>MusicStyle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ music_style.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ music_style.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ music_style.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_musicStyle_index') }}">back to list</a>

    <a href="{{ path('admin_musicStyle_edit', {'id': music_style.id}) }}">edit</a>

    {{ include('admin/music_style/_delete_form.html.twig') }}
{% endblock %}
