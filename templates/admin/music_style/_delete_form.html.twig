<form method="post" action="{{ path('admin_musicStyle_delete', {'id': music_style.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ music_style.id) }}">
    <button class="btn">Delete</button>
</form>
