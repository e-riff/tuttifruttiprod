{% extends 'base.html.twig' %}

{% block title %}{{ music_style.name }} - Modification{% endblock %}

{% block body %}
    <main class="adminForms container-lg">
        <h1>Modifier <span class="text-primary">{{ music_style.name }}</span></h1>

        {{ include('admin/music_style/_form.html.twig', {'button_label': 'Sauvegarder'}) }}

        <div class="d-flex justify-content-start">
            <a class="btn btn-primary me-2" href="{{ path('admin_musicStyle_index') }}">Retour à la liste</a>
            {{ include('admin/music_style/_delete_form.html.twig') }}
        </div>
    </main>
{% endblock %}
