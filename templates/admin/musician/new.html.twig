{% extends 'base.html.twig' %}

{% block title %}{{ 'admin.musician.create'|trans }}{% endblock %}

{% block body %}
    <main class="adminForms container-md">
    <h1 class="text-center"><span class="text-primary">{{ 'admin.musician.create'|trans }}</span></h1>

    {{ include('admin/musician/_form.html.twig') }}
        <div class="mt-2">
            <a class="btn btn-primary me-1" href="{{ path('admin_musician_index') }}">{{ 'action.back_to_list'|trans }}</a>
        </div>
    </main>
{% endblock %}
