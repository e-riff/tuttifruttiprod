<form class="d-flex justify-content-center" method="post" action="{{ path('admin_band_media_delete', {'band': band.id, 'media': media.id}) }}" onsubmit="return confirm('{{ 'confirm.delete_media'|trans|e('js') }}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ media.id) }}">
    <button class="btn btn-danger p-1"><i class="bi bi-trash"></i></button>
</form>
