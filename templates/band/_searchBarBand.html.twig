<div class="searchResultForm">
    {{ form_start(searchBar) }}
    {{ form_widget(searchBar.searchQuery) }}

    <button type="submit"><i class="bi bi-search"></i></button>

    {{ form_end(searchBar) }}
</div>