{% extends 'base.html.twig' %}

{% block title %}{{ 'admin.music_style.edit'|trans({'name': music_style.name}) }}{% endblock %}

{% block body %}
    <h1>{{ 'admin.music_style.edit'|trans({'name': music_style.name}) }}</h1>

    {{ include('music_style_admin/_form.html.twig', {'button_label': 'action.save'|trans}) }}

    <a href="{{ path('admin_musicStyle_index') }}">{{ 'action.back_to_list'|trans }}</a>

    {{ include('music_style_admin/_delete_form.html.twig') }}
{% endblock %}
