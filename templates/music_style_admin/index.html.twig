{% extends 'base.html.twig' %}

{% block title %}MusicStyle index{% endblock %}

{% block body %}
    <h1>MusicStyle index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Slug</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for music_style in music_styles %}
            <tr>
                <td>{{ music_style.id }}</td>
                <td>{{ music_style.name }}</td>
                <td>{{ music_style.slug }}</td>
                <td>
                    <a href="{{ path('app_music_style_admin_show', {'id': music_style.id}) }}">show</a>
                    <a href="{{ path('app_music_style_admin_edit', {'id': music_style.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_music_style_admin_new') }}">Create new</a>
{% endblock %}
