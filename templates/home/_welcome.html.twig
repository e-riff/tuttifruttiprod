<section id="welcome">
    <div class="homeCarousel">
        <div id="homeCarousel" class="carousel slide" data-bs-ride="carousel" data-interval="100">
            <div class="carousel-inner">
                {% for band in bands|filter(band => band.picture) %}
                    <div class="carousel-item {{ band.name|lower == "trio anouman" ? "active" }}" data-bs-interval="{{ swiperTimer }}">
                        <img src="{{ vich_uploader_asset(band)}}" class="d-block w-100" alt="...">
                        {#                    <div class="carousel-caption d-none d-md-block">
                        <h5>First slide label</h5>
                        <p>Some representative placeholder content for the first slide.</p>
                    </div>#}
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
    <div class="homeWelcome">
        <div class="welcomText">
            <h1>
                <mark>BIENVENUE</mark>
            </h1>
            <p class="associationDescription">{{ association.getAssociation.description }}</p>
        </div>
    </div>
</section>