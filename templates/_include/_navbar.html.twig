<nav class="navbar navbar-expand-lg navbar-dark bg-black p-4 shadow-none">
    <div class="container-fluid">
        {% set mots = association.getAssociation.name|trim|split(' ') %}
        <a class="h4 navbar-brand" href="/">
            {% if mots|length == 1 %}
                <span class="text-primary">{{ mots[0]|upper }}</span>
            {% else %}
                {{ mots[0]|upper }}
                <span class="text-primary">{{ mots|slice(1)|join(' ') }}</span>
            {% endif %}
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
                aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <i class="bi bi-chevron-bar-down"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link {{ app.request.pathInfo == path('index') ? "active" : "" }}" href="{{ path('index') }}">Accueil</a>
                <a class="nav-link {{ app.request.pathInfo == path('app_concert_index') ? "active" : "" }}" href="{{ path('app_concert_index') }}">Agenda</a>
                <a class="nav-link {{ app.request.pathInfo == path('band_index') ? "active" : "" }}" href="{{ path('band_index') }}">Les spectacles</a>
                <a class="nav-link {{ app.request.pathInfo == path('musician_index') ? "active" : "" }}" href="{{ path('musician_index') }}">Les musiciens</a>
                <a class="nav-link" href="{{ path('index') }}#contact">Contact</a>
            </div>
        </div>
    </div>
</nav>